# Unity Package Import Harness

This directory documents the minimal Unity import harness generated by
`tests/tools/verify_engine_package_import.py`.

CI uses the generated project to verify that the shipped Unity package artifacts
can be referenced as packages, expose the expected runtime scripts, and carry the
native libraries needed by supported Windows, Android, macOS, and iOS lanes.
