# Vulkan renderer plan

## Current renderer shape

- `piRenderer` is the shared rendering abstraction used by the player, Unity/Godot bridges, and standalone apps.
- Existing backends are selected through `piRenderer::API` and `piRenderer::Create`.
- Android currently defaults to GLES and builds `piGLES_Renderer.cpp` into `libImmCore`.
- The Metal backend is the best template for a new explicit-API backend: it owns backend state, supports externally-owned native frame/device integration, wraps renderer resources, implements the full `piRenderer` surface, and is working well enough for playback in the macOS standalone player, macOS Unity plugin, and macOS Godot plugin.

## Current learning

- Metal is a working playback backend used by the macOS standalone player, macOS Unity plugin, and macOS Godot plugin.
- Vulkan is a blocker for the Godot plugin on Windows and Android.
- The first target should be a Windows standalone Vulkan player because it is the easiest path to verify locally while building out backend behavior.
- Local Vulkan shader tools were installed with Scoop: `dxc`, `glslangValidator`, `glslc`, `spirv-val`, and `spirv-dis`. Build documentation and Windows CI now call out/install the Vulkan SDK toolchain because Vulkan shader generation and validation depend on SPIR-V tools. Android Godot packaging additionally needs Android SDK command-line tools, NDK `28.1.13356709` for Godot 4.5 `godot-cpp`, and NDK `26.1.10909125` for the existing IMM Gradle/CMake modules.
- The static paint GLSL can now be transformed into Vulkan-compatible GLSL, compiled to SPIR-V variants, validated with `spirv-val`, checked in as generated includes, and passed through `CreateShaderBinary` on the Windows Vulkan path.
- The interim Windows standalone CPU raster path was useful as a visibility bridge, but the current `sample1.imm` checkpoint is rendered through Vulkan picture and static-paint GPU draw paths.
- The Vulkan backend now has real device-local `VkImage` allocation and `VkImageView` creation for supported 2D renderer textures, including multisampled render textures, plus swapchain image views, render passes, and framebuffers for both swapchain and renderer-created render targets. Renderer buffers allocate host-visible coherent `VkBuffer` memory for vertex, index, constant, storage, indirect, and transfer use. The Windows standalone path now records Vulkan picture/static-paint draw commands and presents the resulting GPU target through the swapchain.

## Acceptance target

- The Windows standalone player must render `exampleImmFiles/sample1.imm` through `RenderingAPI: "Vulkan"` with visible output comparable to the existing base standalone rendering path.
- Vulkan initialization-only smoke is not sufficient.
- Placeholder diagnostics for render target binding or draw submission are blockers, not acceptable completion criteria.

## Scope for this pass

1. Add `piRenderer::API::Vulkan` without changing existing defaults.
2. Add a Vulkan backend under `code/libImmCore/src/libRender/vulkan`.
3. Make the backend compile in the Windows standalone build first, then keep Android integration as a follow-up target.
4. Implement initialization, teardown, reporting, CPU-side resource wrappers, state tracking, query timing, viewport state, and unsupported-feature diagnostics.
5. Add enough Windows standalone player selection/build wiring to instantiate and verify the Vulkan backend locally.
6. Replace placeholder render target and draw submission paths with real Vulkan rendering for the standalone player.
7. Translate or provide the shader path needed by `sample1.imm` playback.

## Implementation tasks

1. [complete] Create `piVulkan_Renderer.h/.cpp` with the full `piRenderer` implementation surface.
2. [complete] Support two initialization paths:
   - externally supplied Vulkan handles through an opaque config struct;
   - self-created Vulkan instance/device/queue for smoke/build validation.
3. [complete] Add minimal Vulkan resource wrappers for textures, buffers, samplers, render targets, shaders, vertex arrays, fixed states, and CPU timing queries.
4. [complete] Add one-time reporter diagnostics for unsupported shader compilation, draw submission, render target operations, image bindings, compute, atomics, and pixel pack buffers.
5. [complete] Register `API::Vulkan` in `piRenderer::Create` for Windows and Android.
6. [complete] Update Windows project files to build the Vulkan source and link/load the Vulkan loader.
7. [complete] Add Windows standalone viewer selection for Vulkan.
8. [complete] Update shared bridge/Godot API names so Vulkan can be requested explicitly on Windows and Android while preserving existing defaults.
9. [complete] Verify with the closest available Windows standalone build.
10. [complete] Update Android CMake to build the Vulkan source and load `libvulkan.so` dynamically after Windows verification.
11. [complete] Add a separate Windows standalone Vulkan smoke settings file without changing the default viewer config.
12. [complete] Implement real Vulkan render targets, command buffers, render pass/framebuffer setup, and swapchain presentation for the Windows standalone viewer. The owned Windows path creates a Win32 `VkSurfaceKHR`, selects a graphics queue that can present to it, enables `VK_KHR_swapchain`, creates a window-sized swapchain, records picture/static-paint draw command buffers into renderer-created render targets, resolves multisampled color targets for readback/present, and presents the GPU target through the swapchain.
13. [complete] Implement or generate Vulkan-compatible shaders for the player paths exercised by `sample1.imm`. Static paint vertex/fragment SPIR-V variants are generated and validated for Windows Vulkan, and the Windows Vulkan picture/equirect path now has generated SPIR-V shader includes as well.
14. [complete] Verify standalone Vulkan output for `sample1.imm` against the existing base rendering behavior. Release validation-frame Vulkan capture now compares against the Release DirectX baseline with MAE 1.836, RMSE 8.075, and visible-overlap ratio 0.870.
15. [complete] Implement interim CPU raster output for static paint so `sample1.imm` becomes visible in the Windows standalone Vulkan-selected path before the full SPIR-V/pipeline path lands.

## Follow-up work after this pass

- Define the host-frame contract for Vulkan swapchain images or Godot `RenderingDevice` textures.
- Wire the verified Vulkan backend into the Windows Godot plugin path and add plugin-level smoke coverage.
- Expand GLSL/HLSL-to-SPIR-V shader generation beyond the player paths currently exercised by `sample1.imm`.
- Replace the temporary per-present `vkDeviceWaitIdle` synchronization with proper per-frame command/fence ownership.
- Broaden render pass/framebuffer compatibility, pipeline cache keys, and draw submission beyond the picture/static-paint paths covered by `sample1.imm`.
- Add Android Godot export/runtime smoke coverage on a Vulkan-capable Android device or emulator. Android Godot native binary packaging now exists; the remaining gap is a Godot Android export/install/run harness that loads the addon and exercises the Vulkan compositor path.

## Verification notes

- Windows `libImmCore` Debug builds with `piVulkan_Renderer.cpp`.
- Windows `appImmViewer` Debug builds successfully with Vulkan selectable from settings.
- Existing OpenGL standalone smoke reached IMM CPU/GPU load and playback start before manual termination.
- Vulkan standalone smoke using `code/appImmViewer/exe/settings-vulkan-smoke.json` initialized an owned Vulkan device and loaded `sample1.imm`, but did not render before the interim paint/present work. The placeholder diagnostics for render target binding and draw submission are now tracked as blockers.
- Vulkan standalone smoke now reaches static paint CPU rasterization for `sample1.imm`: diagnostic `IMM_VK_CPU` reported projected paint vertices, inside-target samples, visible segments, max alpha 1.0, and a nonblack paint target. The generated capture is visible but currently only an early partial frame, so comparable full-output verification remains in progress.
- Current learning: rasterizing every triangle-strip index as a line sample is redundant and slow for static paint. The interim CPU path is being narrowed to unique source paint points per chunk and throttled presentation so the standalone player can progress toward a fuller frame.
- Current learning: the capture hook initially wrote only the first nonblack paint target, which is too weak for comparable-output verification. `IMM_VULKAN_CPU_CAPTURE_OVERWRITE` now allows smoke tests to keep the latest nonblack paint output.
- Latest Windows Vulkan smoke with overwrite capture ran `sample1.imm` for 45 seconds, produced a visibly fuller paint capture, and had no `not implemented`, render-target placeholder, or draw-submission diagnostics in `debug.txt`. Capture stats: 909,307 nonblack pixels, 127,345 near-visible pixels, max RGB 208,204,173.
- Base-renderer comparison is still not fully established locally: OpenGL and DirectX validation capture attempts both exited with `0x80000003` after import-time/runtime issues before producing a validation image. Comparable-output verification remains in progress until a stable base capture or a stronger visual invariant is available.
- Real Vulkan presentation progress: the Windows standalone owned-device path now requests `VK_KHR_surface`/`VK_KHR_win32_surface`, creates a Win32 `VkSurfaceKHR`, requires queue-family surface support, enables `VK_KHR_swapchain` on the logical device, creates a swapchain, enumerates swapchain images, and records/submits/presents a cleared swapchain frame.
- Latest Windows Vulkan smoke verified the real present path: `debug.txt` reported `Vulkan renderer created frame resources` and `Vulkan renderer presented swapchain frame`, then continued to `Loaded in GPU [0]` for `sample1.imm`.
- Latest Windows Vulkan smoke verified player output through the real present path: after `sample1.imm` loaded and paint rasterization produced a nonblack target, `debug.txt` reported `Vulkan renderer presented swapchain nonblack texture frame`. The runtime GDI blit bridge has been removed; PPM capture remains only as an opt-in verification artifact, while visible paint progress is routed through staging upload and `vkQueuePresentKHR`.
- Latest no-GDI Windows Vulkan smoke verified the same route: `debug.txt` reported `Vulkan renderer presented swapchain nonblack texture frame`; opt-in capture stats were 755,024 nonblack pixels, 160,558 near-visible pixels, max RGB 209,227,217.
- Current implementation progress: supported 2D texture formats now map to Vulkan formats and allocate device-local `VkImage` memory through dynamically loaded `vkCreateImage`, `vkGetImageMemoryRequirements`, and `vkBindImageMemory`; those images now get `VkImageView` handles through dynamically loaded `vkCreateImageView`.
- Latest Windows Vulkan smoke after texture image allocation: single-file compile passed, full `appImmViewer` Debug build passed, and `settings-vulkan-smoke.json` loaded `sample1.imm`, created Win32 surface/swapchain/frame resources, reported `Vulkan renderer created VkImage-backed texture`, and presented a nonblack swapchain texture frame. Opt-in PPM capture stats: 263,504 nonblack pixels, 49 near-visible pixels, max RGB 37,50,20. This proves the image allocation change did not break startup/present, but comparable full-output verification remains open because the captured frame is weaker than the prior paint capture.
- Latest Windows Vulkan smoke after texture image-view allocation: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, reported `Vulkan renderer created VkImage-backed texture view`, and presented a nonblack swapchain texture frame. Opt-in PPM capture stats: 802,810 nonblack pixels, 168,636 near-visible pixels, max RGB 207,227,217. This is back in the same range as the stronger prior paint captures, but still relies on the interim CPU paint raster/staging path rather than real Vulkan graphics pipelines.
- Latest Windows Vulkan smoke after swapchain render-pass/framebuffer setup: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, reported `Vulkan renderer created swapchain render pass and framebuffers`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 921,600 nonblack pixels, 121,274 near-visible pixels, max RGB 166,166,53. This verifies the new frame structure does not regress the visible `sample1.imm` playback checkpoint.
- Latest Windows Vulkan smoke after renderer-created render target framebuffer setup: an initial smoke failed because the viewer's main render textures are multisampled and the Vulkan texture helper was still skipping multisample images. After adding Vulkan sample-count handling for 2x/4x/8x/16x images, full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer created render target framebuffer`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 921,600 nonblack pixels, 16,624 near-visible pixels, max RGB 166,166,53.
- Latest Windows Vulkan smoke after renderer buffer backing: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer created VkBuffer-backed renderer buffer`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 790,522 nonblack pixels, 166,962 near-visible pixels, max RGB 207,227,217. The visible path still uses interim CPU paint rasterization, but static paint vertex/index/constant buffers now have real Vulkan buffer handles ready for draw binding.
- Latest Windows Vulkan smoke after static paint SPIR-V generation: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer created SPIR-V shader modules`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 326,309 nonblack pixels, 15,923 near-visible pixels, max RGB 106,227,218. This verifies shader module creation and preserves visible playback, but rendering still uses the interim CPU paint raster path rather than Vulkan graphics pipelines.
- Current graphics-pipeline learning: static paint will need descriptor bindings for layer/display/chunk constant buffers at 3/4/9, vertex storage data at 8, and the blue-noise sampler array at 7. The Vulkan texture helper is being extended from plain 2D images to 2D-array images/views so the blue-noise resource can be bound by the eventual descriptor set.
- Latest Windows Vulkan smoke after 2D-array image/view support: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` created the existing Vulkan resources plus SPIR-V shader modules, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 656,720 nonblack pixels, 125,292 near-visible pixels, max RGB 208,227,217. This verifies the blue-noise 2D-array image/view groundwork did not regress visible playback.
- Current graphics-pipeline learning: combined image-sampler descriptors need a real `VkSampler`; texture creation and explicit sampler creation now create/destroy Vulkan sampler handles using the renderer's texture filter/wrap settings.
- Latest Windows Vulkan smoke after sampler creation: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` created Vulkan resources and SPIR-V modules, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 921,600 nonblack pixels, 181,127 near-visible pixels, max RGB 209,227,217.
- Current graphics-pipeline progress: CPU-backed textures with initial data now upload that data into their Vulkan images through a one-shot staging-buffer command, then transition to `VK_IMAGE_LAYOUT_SHADER_READ_ONLY_OPTIMAL`. This gives the future static-paint descriptor set a populated blue-noise image rather than only CPU-side bytes.
- Latest Windows Vulkan smoke after initial texture-image upload: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 921,600 nonblack pixels, 180,899 near-visible pixels, max RGB 208,227,217.
- Current graphics-pipeline progress: static paint SPIR-V shader creation now also creates a descriptor set layout and pipeline layout matching the player binding contract: layer/display/chunk uniform buffers at 3/4/9, vertex storage buffer at 8, and blue-noise combined image sampler at 7. This is still layout groundwork only; descriptor allocation/update, graphics pipeline creation, and `vkCmdDrawIndexed` submission remain open.
- Latest Windows Vulkan smoke after static paint descriptor/pipeline layout creation: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer created static paint descriptor and pipeline layouts`, reported `Vulkan renderer created SPIR-V shader modules`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 921,600 nonblack pixels, 128,615 near-visible pixels, max RGB 209,207,173.
- Current graphics-pipeline progress: the static paint descriptor pool/set is now allocated from the layout and updated during `DrawPrimitiveIndexed` from the active renderer bindings. The descriptor set now points at the layer/display/chunk uniform buffers, static paint vertex storage buffer, and uploaded blue-noise image/sampler. This is still descriptor binding groundwork; graphics pipeline creation and `vkCmdDrawIndexed` submission remain open.
- Latest Windows Vulkan smoke after descriptor set allocation/update: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer updated static paint descriptor set`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced a complete opt-in PPM capture with 921,600 nonblack pixels, 2,330 near-visible pixels, max RGB 155,156,44.
- Current graphics-pipeline progress: static paint now creates a real Vulkan graphics pipeline for the active SPIR-V shader modules and renderer-created render pass. The pipeline uses a triangle-strip input assembly, no vertex attributes because static paint reads vertex data through binding 8, dynamic viewport/scissor, raster state derived from the current paint raster state, alpha blending, the static paint pipeline layout, and the render target's sample count. Command-buffer render-pass recording, descriptor/pipeline binding, index binding, and `vkCmdDrawIndexed` remain open.
- Latest Windows Vulkan smoke after graphics pipeline creation: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer created static paint graphics pipeline`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced a complete opt-in PPM capture with 921,600 nonblack pixels, 2,330 near-visible pixels, max RGB 155,156,44.
- Current graphics-pipeline progress: static paint now records and submits a real Vulkan command buffer for the active draw call. The submitted commands begin the renderer-created render pass, set dynamic viewport/scissor, bind the static paint graphics pipeline, bind the static paint descriptor set, bind the index buffer, issue `vkCmdDrawIndexed`, end the render pass, submit to the graphics queue, and wait for completion. The CPU raster fallback still runs afterward and remains the visible presentation source, so GPU-rendered output is not yet independently verified.
- Latest Windows Vulkan smoke after static paint draw submission: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer submitted static paint draw commands`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced a complete opt-in PPM capture with 921,600 nonblack pixels, 2,330 near-visible pixels, max RGB 155,156,44.
- Current graphics-pipeline progress: Vulkan-owned render images are temporarily created as single-sample images for the first Windows standalone GPU-output verification, even when the viewer requests MSAA. The `C3_11_11_10_FLOAT` color target is also backed by an `R8G8B8A8_UNORM` Vulkan image for now so readback and swapchain staging can use the same byte layout as the existing verification path. This is a verification bridge, not the final MSAA/HDR resolve path.
- Latest Windows Vulkan smoke after static paint GPU readback: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer submitted static paint draw commands`, then `Vulkan renderer read back static paint GPU target nonblack=3020 size=1280x720`, then `Vulkan renderer presented swapchain nonblack texture frame`. The first opt-in PPM capture was complete and matched the GPU-readback path with 3,020 nonblack pixels, 2,397 near-visible pixels, max RGB 112,107,8. This proves the real Vulkan draw path produces visible pixels, but output is still sparse and not yet comparable to the prior CPU fallback/base-renderer output.
- Current graphics-pipeline progress: GPU readback/capture is now deferred until 128 accumulated static paint draw calls, and the older CPU-present capture is suppressed while a Vulkan paint readback is pending. This makes the verification artifact come from accumulated GPU-rendered paint instead of the first sparse chunk or the CPU fallback.
- Latest Windows Vulkan smoke after deferred GPU readback: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer submitted static paint draw commands`, then `Vulkan renderer read back static paint GPU target nonblack=232144 size=1280x720`. The opt-in PPM capture was complete and matched the GPU-readback path with 232,144 nonblack pixels, 204,958 near-visible pixels, max RGB 166,202,186. This is direct Vulkan-rendered output, but still below the stronger CPU fallback/base-output range.
- Current graphics-pipeline progress: opt-in PPM capture now writes through a temporary file and renames into place, so overwrite capture can keep later GPU readbacks without leaving a truncated final image if the smoke process is stopped mid-write. The generic fullscreen resolve/present path is also suppressed while Vulkan paint GPU output is active, so the verification artifact remains a GPU-readback artifact rather than being overwritten by the CPU-backed resolve texture.
- Latest Windows Vulkan smoke with overwrite GPU capture: full `appImmViewer` Debug build passed, repeated static-paint GPU readbacks improved over time up to `nonblack=356882` at draw 9728. The final complete PPM capture had 356,547 nonblack pixels, 316,595 near-visible pixels, max RGB 166,202,186. A `.tmp` artifact can remain if the process is force-stopped during a later atomic write, but the final PPM remains complete.
- Current graphics-pipeline progress: when the static-paint Vulkan graphics path is active, `DrawPrimitiveIndexed` now returns before the interim CPU paint raster fallback. Readback is deferred to 512-draw intervals so smoke verification is driven by accumulated GPU-rendered output rather than CPU raster output or an early sparse chunk.
- Latest Windows Vulkan GPU-only smoke after bypassing the CPU paint fallback: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, submitted real Vulkan static-paint draw commands, and repeatedly read back the Vulkan render target. The final complete opt-in PPM capture had 418,761 nonblack pixels, 215,878 near-visible pixels, max RGB 166,202,186, with the last logged GPU readback at `nonblack=418761` after draw 87552. Visual inspection of the converted capture shows the `sample1.imm` scene with the central figure, colored strokes, and long paint/guide lines rendered from the Vulkan path. This is the current standalone Vulkan rendering checkpoint; remaining work is to tighten parity with the base renderer.
- Current graphics-pipeline progress: renderer-created 2D Vulkan images now honor requested MSAA sample counts again instead of forcing every image to one sample. Static-paint readback handles multisampled targets by resolving the render target into a temporary single-sample Vulkan image with `vkCmdResolveImage`, then copying that resolved image into the staging buffer used by the PPM verification path.
- Latest Windows Vulkan MSAA smoke: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, submitted real Vulkan static-paint draw commands, resolved/read back the multisampled Vulkan render target, and produced a complete opt-in PPM capture with 446,711 nonblack pixels, 198,920 near-visible pixels, max RGB 166,202,187. Visual inspection of the converted capture still shows the `sample1.imm` scene, figure, colored strokes, and long paint/guide lines through the Vulkan path.
- Current graphics-pipeline progress: `C3_11_11_10_FLOAT` render textures now use Vulkan's packed `VK_FORMAT_B10G11R11_UFLOAT_PACK32` instead of the temporary `R8G8B8A8_UNORM` backing. The readback/capture bridge decodes the packed 11/11/10 float bits into the existing RGBA8 CPU buffer only after GPU rendering and resolve.
- Latest Windows Vulkan packed-format smoke: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, submitted real Vulkan static-paint draw commands, resolved/read back the MSAA packed 11/11/10 render target, and produced a complete opt-in PPM capture with 437,639 nonblack pixels, 169,041 near-visible pixels, max RGB 165,201,183. Visual inspection of the converted capture shows the expected `sample1.imm` figure, colored strokes, and long paint/guide lines without the channel-swap artifact from the first packed-format attempt.
- Current graphics-pipeline progress: swapchain presentation now has a direct GPU path for Vulkan paint targets. `SwapBuffers` resolves a multisampled paint image when needed, blits the single-sample Vulkan image directly into the acquired swapchain image with `vkCmdBlitImage`, transitions the swapchain image to present, and only uses the CPU staging upload path as a fallback.
- Latest Windows Vulkan direct-present smoke: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, logged `Vulkan renderer presented swapchain direct GPU texture frame`, continued real static-paint GPU readbacks, and produced a complete opt-in PPM capture with 438,271 nonblack pixels, 181,248 near-visible pixels, max RGB 165,203,183. Visual inspection of the converted capture still shows the expected `sample1.imm` figure, colored strokes, and long paint/guide lines.
- Current graphics-pipeline progress: direct swapchain presentation is now decoupled from CPU readback. The Vulkan paint path presents the GPU texture every 128 accumulated static-paint draws, while the CPU readback/capture path runs every 1024 draws for verification only.
- Latest Windows Vulkan present-cadence smoke: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, logged `Vulkan renderer presented swapchain direct GPU texture frame`, read back at 1024-draw intervals, and produced a complete opt-in PPM capture with 446,363 nonblack pixels, 183,064 near-visible pixels, max RGB 165,201,183. Visual inspection of the converted capture shows the expected scene content. Android `:libImmCore:assembleDebug` also completed successfully after this cadence change.
- Current verification progress: `code/appImmViewer/scripts/run-vulkan-sample1-smoke.ps1` now provides a repeatable Windows standalone Vulkan gate. It builds `appImmViewer`, runs `settings-vulkan-smoke.json`, requires CPU/GPU load completion, direct GPU swapchain presentation, Vulkan GPU paint readback, no Vulkan placeholder/failure diagnostics, and a complete 1280x720 PPM capture above visible-pixel thresholds.
- Latest scripted Windows Vulkan smoke: `run-vulkan-sample1-smoke.ps1 -SkipBuild` passed against the current Debug viewer with 444,900 nonblack pixels, 172,483 near-visible pixels, max RGB 165,201,183. Android `:libImmCore:assembleDebug` also completed successfully after adding the smoke script/docs.
- Baseline Windows DirectX smoke using `settings-baseline-directx-sample1.json` still reaches `Loaded in CPU` and `Loaded in GPU [0]` after the Vulkan-only fallback bypass. The matched AudioSDK seek failures are unchanged runtime audio diagnostics and not a Vulkan renderer regression.
- Current comparable-output learning: the Windows DirectX baseline capture script must use a current build. The stale `appImmViewer_Release.exe` from March 2026 loaded `sample1.imm` but never reached validation. `capture_windows_directx_baseline.ps1` now builds/uses the current `appImmViewer_Release.exe` by default, has a bounded timeout, writes a PPM baseline, and the latest DirectX baseline capture passed with validation frame 36, 921,600 nonzero pixels, 38 draw calls, 37 paint draw calls, one 360-picture draw call, and 645,802 triangles.
- Current picture-pipeline progress: Windows Vulkan picture layers now use generated SPIR-V equirect shaders, a Vulkan picture descriptor/pipeline layout, vertex input derived from `CreateVertexArray2`, and indexed triangle submission. The equirect background orientation is corrected through Vulkan's negative-height viewport convention.
- Current composition progress: Vulkan standalone display render draws picture layers before paint layers so the 360 image behaves as a background while static paint remains visible. Vulkan `Clear(..., depth0=true)` now clears depth images to `1.0`, matching the DirectX clear contract; explicit Vulkan command helpers currently wait the device idle before reusing the shared standalone command/fence path, which is acceptable for this verification milestone but should be replaced by proper per-frame synchronization.
- Latest Windows Release Vulkan smoke after picture/depth/order work: Release build passes and `run-vulkan-sample1-smoke.ps1 -Configuration Release -SkipBuild -KeepArtifacts -DurationSeconds 45` reaches CPU/GPU load, static paint draw submission, picture draw submission, and direct swapchain presentation. Smoke capture stats: 833,687 nonblack pixels, 116,083 near-visible pixels, max RGB 179,239,255.
- Comparable-output gate passed: `capture_windows_directx_baseline.ps1 -Configuration Release -SkipBuild -TimeoutSeconds 90` captured the DirectX baseline at validation frame 42. `capture_windows_vulkan_validation.ps1 -Configuration Release -SkipBuild -TimeoutSeconds 90` captured the Vulkan candidate at validation frame 10 with one picture draw, 37 paint draw calls, 645,802 triangles, and a GPU target readback of 899,376 nonblack pixels. `compare-ppm-captures.ps1` passed for `windows-directx-static.ppm` vs `windows-vulkan-validation.ppm`: MAE 1.836, RMSE 8.075, visible-overlap ratio 0.870.
- Android verification after this slice: `.\gradlew.bat :libImmCore:assembleDebug` passes after guarding the Windows-only PPM capture call from Android compilation and after the final synchronization/display-order changes.
- Current comparable-output tooling: `compare-ppm-captures.ps1` compares baseline and Vulkan PPM captures by dimensions, mean absolute error, RMSE, max channel error, visible-pixel counts, and visible overlap. The PPM readers now consume only the single post-header delimiter so binary pixel bytes with whitespace values are not mistaken for header whitespace.
- Current Vulkan rendering progress: render-target `Clear` now clears the Vulkan color image as well as the CPU shadow buffer. This fixed the earlier multi-frame paint accumulation/long-streak artifact. Static-paint draw calls now update the pending present texture without presenting mid-frame; readback/capture is performed from the presented GPU target during `SwapBuffers`.
- Latest Windows Vulkan smoke after GPU color clears and frame-end present capture: full `appImmViewer` Debug build passed. `run-vulkan-sample1-smoke.ps1 -SkipBuild -KeepArtifacts -DurationSeconds 45` produced a stable `sample1.imm` capture showing the character and paint geometry without the prior accumulated streaks. The strict smoke thresholds from the accumulated-paint checkpoint now fail because the capture is a true cleared frame and Vulkan still does not render the 360 picture backdrop.
- Current blocker for baseline parity: DirectX baseline includes the `sample1.imm` 360 picture/background layer (`pictureDrawCalls=1`, `picture360EquirectDrawCalls=1`), while the Vulkan backend currently renders only the static-paint path. The latest comparison against the DirectX baseline had MAE 20.126 and RMSE 52.183, but only 9.6% reference-visible overlap because the picture backdrop is absent. Next work is enough Vulkan picture rendering for `sample1.imm`, then re-run the comparison gate.
- Current picture-layer progress: Vulkan now has an explicitly logged interim `IMM_VK_CPU: picture fallback` path for indexed triangle picture draws. It samples the bound picture texture into the current color target CPU shadow buffer, packs/uploads that buffer back into the Vulkan color attachment, and lets the existing Vulkan static-paint GPU path draw over it. This makes the `sample1.imm` backdrop visible in the Vulkan standalone capture, but it is still not the final solution because it does not yet run the picture shader/equirect projection on the GPU.
- Latest Windows Vulkan smoke after the interim picture fallback: full `appImmViewer` Debug build passed and `run-vulkan-sample1-smoke.ps1 -SkipBuild -KeepArtifacts -DurationSeconds 45` passed with 921,600 nonblack pixels, 64,278 near-visible pixels, max RGB 165,165,183. The DirectX comparison still fails: MAE 22.716, RMSE 53.897, visible overlap 10.9%. The remaining parity work is a real Vulkan picture/equirect shader pipeline rather than the texture-copy fallback.
- Current picture-layer progress: Windows Vulkan now creates a real picture/equirect SPIR-V shader path for `sample1.imm`, with descriptor bindings for the picture texture/sampler plus layer/display constant buffers, vertex input attributes for the generated dome mesh, a picture graphics pipeline, vertex/index binding, and indexed `vkCmdDrawIndexed` submission. `debug.txt` now reports `Vulkan renderer created picture descriptor and pipeline layouts`, `updated picture descriptor set`, `created picture graphics pipeline`, and `submitted picture draw commands`; the interim CPU picture fallback is not used on this path.
- Latest Windows Vulkan smoke after the GPU picture path: full `appImmViewer` Debug build passed and `run-vulkan-sample1-smoke.ps1 -SkipBuild -KeepArtifacts -DurationSeconds 45` passed with 921,600 nonblack pixels, 29,240 near-visible pixels, max RGB 165,173,183. The resulting capture shows the 360 picture background, character, and paint strokes through Vulkan GPU paths. The strict DirectX PPM comparison still fails because the validation frames/camera pose do not align closely enough: latest MAE 19.864, RMSE 52.407, visible overlap 8.7%. Comparable-output work remains open, but the standalone player is no longer CPU-rendering the picture or static paint paths for this checkpoint.
- Build-tool documentation update: `code/projects/windows/README.md` now lists both checked-in Vulkan shader generators, including the new picture generator. Windows CI already installs `vulkan-sdk` and verifies `glslangValidator.exe` and `spirv-val.exe` before building.
- Latest Windows Vulkan Release playback fix: normal `SwapBuffers` no longer performs GPU readback unless an explicit capture path is requested, early validation probes avoid GPU readback until Vulkan paint has drawn, and direct swapchain texture presentation is gated until a real paint draw has occurred. This prevents validation/capture behavior from dominating live playback and avoids pre-load render-target layout/fence failures.
- Latest Windows Vulkan Release smoke: `run-vulkan-sample1-smoke.ps1 -Configuration Release -SkipBuild -KeepArtifacts -DurationSeconds 30` passed with 832,785 nonblack pixels, 48,802 near-visible pixels, and max RGB 133,185,255. This is brighter than the prior dark diagnostic run and still uses GPU picture/static-paint draw paths.
- Latest Windows Vulkan no-capture playback sample: launching `appImmViewer_Release.exe settings-vulkan-smoke.json` without `IMM_VULKAN_CPU_CAPTURE_PATH` and sampling the window title for 10 seconds reported roughly 252-332 fps after startup. The remaining audio seek failures in `debug.txt` are the existing Windows AudioSDK diagnostics, not Vulkan renderer failures.
- Latest DirectX/Vulkan validation comparison: DirectX Release baseline capture and Vulkan Release validation capture both passed. `compare-ppm-captures.ps1` reported MAE 3.114, RMSE 10.462, maxAbs 173, reference visible 162,242, candidate visible 133,628, overlap 126,495, and overlapRatio 0.780.
- Latest Vulkan validation log evidence: frame 9 had 897,982 nonzero pixels, 38 draw calls, 37 paint draw calls, one picture draw call, one 360/equirect picture draw call, 645,802 triangles, and a successful validation capture at `build/baseline-captures/windows-vulkan-validation.ppm`.
- Latest Windows Vulkan smoke correction: `run-vulkan-sample1-smoke.ps1` no longer uses the continuous `IMM_VULKAN_CPU_CAPTURE_PATH` readback path that caused the slow/dark diagnostic run. It now performs a deterministic validation-frame capture, optionally compares it to a DirectX PPM reference, then runs a short no-capture live-present pass that must log direct GPU swapchain presentation.
- Latest Windows Vulkan Release smoke after deterministic capture/live-present split and present synchronization: Release build passed. `run-vulkan-sample1-smoke.ps1 -Configuration Release -SkipBuild -KeepArtifacts -DurationSeconds 90 -PresentSeconds 8 -ReferencePath .\build\baseline-captures\windows-directx-static.ppm` passed with 899,376 nonblack pixels, 150,335 near-visible pixels, max RGB 255,255,255; DirectX comparison passed with MAE 1.836, RMSE 8.075, and visible-overlap ratio 0.870; the live-present phase logged direct GPU Vulkan presentation without Vulkan failure diagnostics.
- Latest Windows Vulkan no-capture playback sample after adding the temporary post-present device idle: launching `appImmViewer_Release.exe settings-vulkan-smoke.json` without capture env and sampling the window title for roughly 7 seconds reported about 190-228 fps after startup. This confirms the fix did not regress into the earlier `4.7fps` capture-bound behavior, but proper per-frame synchronization should replace the coarse device idle before broader plugin/runtime work.
- Latest Android build after the deterministic smoke/synchronization update: `.\gradlew.bat :libImmCore:assembleDebug` completed successfully from `code/projects/android`.
- Latest teardown fix: `piRenderMesh::End` now destroys `mVAO[j]` for each element-array vertex array instead of repeatedly indexing `mVAO[i]`, fixing a shutdown heap corruption hit by the Vulkan validation path.
- Current performance/testing learning: the viewer window-title FPS counter is useful as an app-loop smoke signal but is not a reliable cross-API benchmark. A hidden-window DirectX run reported roughly 4,734 fps, which is likely an uncapped/counter artifact rather than comparable presented rendering. A hidden-window Vulkan Release run initially stayed around 58-60 fps even with `VK_PRESENT_MODE_IMMEDIATE_KHR` selected. After making the owned standalone `SwapBuffers` submit wait before reusing the shared command buffer/fence, Release Vulkan no-capture playback sampled 166.5-234.8 fps with a 192.6 fps average and no Vulkan failure diagnostics in the sampled log.
- Current verification progress: `run-vulkan-sample1-smoke.ps1` now has two phases. The first uses the viewer validation capture path with audio disabled to require CPU/GPU load, picture and paint draw coverage, Vulkan GPU readback, a complete 1280x720 capture, and no Vulkan failure diagnostics. The second runs live presentation without capture and requires direct GPU swapchain presentation with no Vulkan failure diagnostics. Latest Release run passed both phases with capture stats of 899,376 nonblack pixels, 150,335 near-visible pixels, and max RGB 255,255,255.
- Current scope status: the Windows standalone Vulkan player renders and validates `sample1.imm` comparably to the DirectX baseline, the Windows Godot plugin smoke loads `sample1.imm` through the Godot Vulkan compositor path, the Android non-VR standalone Vulkan APK passes a strict emulator smoke for `sample1.imm` with real picture/static-paint Vulkan draw submission and direct GPU texture presentation, and Android Godot addon binaries now build/package into the manifest-declared locations. The broader active goal is still not complete until Android Godot runtime export/install/run coverage exists and passes.
- Current Godot plugin progress: the Windows/macOS GDExtension diagnostics now report explicit Vulkan candidacy fields (`wants_vulkan_renderer`, `driver_is_vulkan`, `has_vulkan_driver_resources`, and `vulkan_adapter_candidate`) alongside the existing Metal diagnostics. The Windows smoke wrapper can pass renderer API 5 into the native smoke scene, stage the native dependency DLL directory into `PATH`, and require a post-load Vulkan compositor render instead of accepting initialization-only handoff evidence.
- Latest Godot verification after Vulkan diagnostics: `python code\appImmGodotGDExtension\verify_local.py` passed, and `MSBuild code\projects\windows\imm.sln -t:appImmGodot -p:Configuration=Release -p:Platform=x64` built `ImmGodotPlugin.dll` successfully.
- Current Vulkan external-device progress: the shared Vulkan renderer can now initialize command/fence frame resources against a host-provided Vulkan instance/device/queue without requiring a swapchain. Renderer-owned Vulkan resources are destroyed during teardown even when the host owns the `VkDevice`, while the host device itself is left intact. This is groundwork for a Godot `RenderingDevice` handoff; it is not yet a complete Godot Vulkan frame integration.
- Current local Godot tooling state: `thirdparty\godot-cpp` has been bootstrapped for Godot 4.5-stable and a local Godot 4.5-stable console executable is available under `thirdparty\tools`. Headed Windows Godot Vulkan smoke passes; the remaining local Godot limitation is the headless runner crash on shutdown, which is tracked separately from Vulkan render correctness.
- Latest verification after external-device frame-resource work: `python code\appImmGodotGDExtension\verify_local.py` passed, Release `appImmGodot` and Release `appImmViewer` both built, the Godot build helper preflight passed with `-BootstrapGodotCpp -BuildGodotCpp -PreflightOnly`, and the Release Vulkan `sample1.imm` smoke still passed against the DirectX reference with MAE 1.836, RMSE 8.075, visible-overlap ratio 0.870, and direct GPU Vulkan presentation logged.
- Current Godot Vulkan handoff progress: Windows native Godot initialization now defers Vulkan renderer/player graphics initialization until the compositor can pass Godot-owned Vulkan driver resources. `ImmGodot_BeginVulkanFrame` accepts Godot's `VkInstance`, `VkPhysicalDevice`, `VkDevice`, graphics queue, queue family, and target image handles; the GDExtension compositor retrieves those through `RenderingDevice.get_driver_resource` and records diagnostics for the handoff.
- Latest Godot Vulkan verification: local SCons was installed with `python -m pip install --user scons`, `thirdparty\godot-cpp` was bootstrapped at `godot-4.5-stable`, Godot 4.5-stable was installed locally under `thirdparty\tools`, and `build-godot-extension.ps1 -Configuration Release -BuildGodotCpp` passed. `run-godot-smoke.ps1 -Configuration Release -GodotExe .\thirdparty\tools\godot-4.5-stable\Godot_v4.5-stable_win64_console.exe -RequireExtension -RendererApi 5 -Headed -LogDir .\build\logs\godot-vulkan-headed-smoke` now passes after loading `sample1.imm`: Godot reported Vulkan 1.4.329 Forward+ on NVIDIA RTX 4090, backend diagnostics reported `vulkan_adapter_candidate=true`, post-load compositor diagnostics reported 734 callbacks, `last_vulkan_frame_started=true`, `last_composite_result=true`, `last_render_result=0`, and a 1600x900 render for camera 0. The native log for the same run records `Loaded in CPU`, `Loaded in GPU [0]`, `Vulkan renderer submitted picture draw commands`, and `Vulkan renderer submitted static paint draw commands`.
- Latest Godot smoke learning: the old `../../../exampleImmFiles/sample1.imm` native scene path failed because the C++ resolver only globalizes `res://` and `user://` paths. The native scene/sample default now uses `res://../../exampleImmFiles/sample1.imm`, matching the Metal visual smoke path. The first successful load then exposed a Windows delay-load dependency gap at the JPEG layer; `run-godot-smoke.ps1` now prepends the staged GDExtension DLL directory to `PATH` for `-RequireExtension` runs so delayed native dependencies are resolvable after startup.
- Current Windows helper fix: `build-godot-extension.ps1` now builds the `appImmGodot` target without solution-level rebuild parallelism and with `TrackFileAccess=false`/`CL_MPCount=1`, matching the deterministic build shape used during local verification and avoiding the `libImmCore.lib`/tlog races seen with `/t:appImmGodot:Rebuild /m`.
- Latest regression checks after the Godot Vulkan handoff: Release standalone `run-vulkan-sample1-smoke.ps1 -SkipBuild -ReferencePath .\build\baseline-captures\windows-directx-static.ppm` still passes with 899,376 nonblack pixels, 150,335 near-visible pixels, MAE 1.836, RMSE 8.075, visible-overlap ratio 0.870, and direct GPU presentation logged. Android `:libImmCore:assembleDebug` still completes successfully.
- Latest Android Vulkan app build: the Android SDK, platform-tools, and NDK `26.1.10909125` are available locally, but `adb devices` currently reports no connected devices. The non-VR Android Vulkan build path now has a documented Gradle invocation and `run-android-vulkan-smoke.ps1` wrapper. `.\gradlew.bat :libImmCore:assembleDebug :libImmImporter:assembleDebug :libImmPlayer:assembleDebug -PimmBuildDir=build_vulkan_nonvr` plus `.\gradlew.bat :appImmViewer:assembleDebug -PimmNonVr=ON -PimmBuildDir=build_vulkan_nonvr -PimmRendererApi=Vulkan` both pass. This proves the Android app Vulkan selection compiles and packages; runtime smoke remains pending until a Vulkan-capable Android device or emulator is connected.
- Latest Android build after the interim picture fallback: `:libImmCore:assembleDebug` still completes successfully with the shared Vulkan source compiled into the Android library.
- Latest Android build after the packed-format work: `:libImmCore:assembleDebug` initially exposed that swapchain fallback dimensions were kept in Windows-only state fields. Moving those dimensions into platform-neutral Vulkan state fixed the shared build; `:libImmCore:assembleDebug` now completes successfully again.
- Latest Android build after direct-present work: `:libImmCore:assembleDebug` completes successfully with the shared `vkCmdBlitImage` command binding and direct-present code compiled into `piVulkan_Renderer.cpp`.
- Android `:libImmCore:assembleDebug` builds successfully with `piVulkan_Renderer.cpp` included; CMake still emits the pre-existing dev warning about no top-level `project()` command.
- Latest Android build after the validation/readback/present fixes: `:libImmCore:assembleDebug` completed successfully from `code/projects/android`.
- Latest Android build after the present-mode/synchronization smoke fix: `:libImmCore:assembleDebug` completed successfully from `code/projects/android`.
- Latest Android runtime smoke: a standard Android emulator was booted and `run-android-vulkan-smoke.ps1 -SkipBuild -WaitSeconds 45` passed against the Vulkan non-VR APK. The log proves `IMM Android renderer API: Vulkan`, Android `VkSurfaceKHR` creation, owned-device/swapchain/frame-resource initialization, Android Vulkan render-target creation, CPU and GPU load of `sample1.imm`, SPIR-V shader module creation, `Vulkan renderer submitted picture draw commands`, `Vulkan renderer submitted static paint draw commands`, and direct GPU texture presentation. The strict smoke now fails on renderer failure markers and on the Vulkan placeholder draw-submission diagnostic.
- Latest Android Vulkan implementation learning: the non-VR Android loop needed a renderer-created color/depth render target before calling `RenderMono`, because the Vulkan draw path cannot target the Android window like GLES targets the default framebuffer. Android Vulkan now binds that offscreen target, unbinds before `SwapBuffers`, and presents the pending GPU texture through the existing swapchain path. Android 2D picture layers now use a non-indexed `vkCmdDraw` picture-quad path keyed by the `PICTURE_2D` shader option, while 360 picture meshes continue through indexed `vkCmdDrawIndexed`.
- Latest Android visual check: a post-smoke `adb exec-out screencap -p` capture at `build/logs/android-vulkan-smoke-screen.png` shows visible rendered paint geometry from `sample1.imm` rather than a blank frame or background-only output.
- Latest Android Vulkan shader learning: Android GLES still uses the ES GLSL shader sources, but Android Vulkan now uses the checked-in SPIR-V shader modules for the `sample1.imm` picture and static-paint paths. The picture dome/cubemap render meshes also use the Vulkan `InitFromMeshWithShader` path on Android so indexed picture draw submission is real rather than falling through to unsupported draw diagnostics.
- Latest Windows regression after Android runtime work: Release standalone `run-vulkan-sample1-smoke.ps1 -Configuration Release -SkipBuild -KeepArtifacts -DurationSeconds 90 -PresentSeconds 8 -ReferencePath .\build\baseline-captures\windows-directx-static.ppm` still passes with 899,376 nonblack pixels, 150,335 near-visible pixels, max RGB 255,255,255, MAE 1.836, RMSE 8.075, visible-overlap ratio 0.870, and direct GPU Vulkan presentation logged.
- Latest Windows Godot regression after Android runtime work: `run-godot-smoke.ps1 -Configuration Release -GodotExe .\thirdparty\tools\godot-4.5-stable\Godot_v4.5-stable_win64_console.exe -RequireExtension -RendererApi 5 -Headed` passed. Godot reported Vulkan Forward+ on the RTX 4090, backend diagnostics confirmed `wants_vulkan_renderer=true` and `vulkan_adapter_candidate=true`, post-load compositor diagnostics reported 651 callbacks with `last_vulkan_frame_started=true`, `last_composite_result=true`, `last_render_result=0`, and a 1600x900 render target.
- Latest Android Godot packaging progress: `code/projects/android/appImmGodot` now builds the native `libImmGodotPlugin.so` and `libimm_godot_extension.arm64.so` libraries for the existing `.gdextension` Android manifest entries. `build-godot-extension-android.ps1 -Configuration Debug -BuildGodotCpp` bootstrapped/built `godot-cpp` for `platform=android target=template_debug arch=arm64`, built the IMM Android static libraries, built the Godot native/GDExtension shared libraries, staged them under `code/ImmGodotSampleProject/addons/imm_viewer/bin/android/debug/`, and verified both staged outputs.
- Latest Android tooling update: the local Android SDK did not include command-line tools and Godot 4.5 `godot-cpp` required NDK `28.1.13356709`. Command-line tools `14742923` and NDK `28.1.13356709` were installed locally; CI now installs/caches both NDK `26.1.10909125` and `28.1.13356709`, installs SCons, builds Android `godot-cpp`, builds the Android Godot GDExtension module, and uploads the staged Android Godot platform binaries.
- Latest regression after Android Godot packaging: `python code\appImmGodotGDExtension\verify_local.py` passed, `build-godot-extension-android.ps1 -Configuration Debug -PreflightOnly` passed, `run-android-vulkan-smoke.ps1 -SkipBuild -WaitSeconds 45` passed on `emulator-5554`, Release Windows standalone Vulkan `sample1.imm` comparison still passed with MAE 1.836/RMSE 8.075/visible-overlap 0.870, and the headed Windows Godot Vulkan smoke still passed with `last_vulkan_frame_started=true`, `last_composite_result=true`, and `last_render_result=0`.
- Current Android Godot gap: Android addon packaging is verified, but Android Godot runtime is still unverified because there is not yet an in-tree Godot Android export/install/run smoke harness that loads the addon and drives the Vulkan compositor path on device.
