# Vulkan renderer plan

## Current renderer shape

- `piRenderer` is the shared rendering abstraction used by the player, Unity/Godot bridges, and standalone apps.
- Existing backends are selected through `piRenderer::API` and `piRenderer::Create`.
- Android currently defaults to GLES and builds `piGLES_Renderer.cpp` into `libImmCore`.
- The Metal backend is the best template for a new explicit-API backend: it owns backend state, supports externally-owned native frame/device integration, wraps renderer resources, implements the full `piRenderer` surface, and is working well enough for playback in the macOS standalone player, macOS Unity plugin, and macOS Godot plugin.

## Current learning

- Metal is a working playback backend used by the macOS standalone player, macOS Unity plugin, and macOS Godot plugin.
- Vulkan is a blocker for the Godot plugin on Windows and Android.
- The first target should be a Windows standalone Vulkan player because it is the easiest path to verify locally while building out backend behavior.
- Local Vulkan shader tools were installed with Scoop: `dxc`, `glslangValidator`, `glslc`, `spirv-val`, and `spirv-dis`. Build documentation and Windows CI now call out/install the Vulkan SDK toolchain because Vulkan shader generation and validation depend on SPIR-V tools.
- The static paint GLSL can now be transformed into Vulkan-compatible GLSL, compiled to SPIR-V variants, validated with `spirv-val`, checked in as generated includes, and passed through `CreateShaderBinary` on the Windows Vulkan path.
- To get standalone visual output sooner, the current implementation includes an interim Windows standalone CPU raster path for the static paint data exercised by `sample1.imm`, presented through the Vulkan-selected renderer until real Vulkan command buffers and pipelines are available.
- The Vulkan backend now has real device-local `VkImage` allocation and `VkImageView` creation for supported 2D renderer textures, including multisampled render textures, plus swapchain image views, render passes, and framebuffers for both swapchain and renderer-created render targets. Renderer buffers now also allocate host-visible coherent `VkBuffer` memory for vertex, index, constant, storage, indirect, and transfer use. These are the frame/resource structures needed for replacing CPU render-target storage with Vulkan graphics pipelines.

## Acceptance target

- The Windows standalone player must render `exampleImmFiles/sample1.imm` through `RenderingAPI: "Vulkan"` with visible output comparable to the existing base standalone rendering path.
- Vulkan initialization-only smoke is not sufficient.
- Placeholder diagnostics for render target binding or draw submission are blockers, not acceptable completion criteria.

## Scope for this pass

1. Add `piRenderer::API::Vulkan` without changing existing defaults.
2. Add a Vulkan backend under `code/libImmCore/src/libRender/vulkan`.
3. Make the backend compile in the Windows standalone build first, then keep Android integration as a follow-up target.
4. Implement initialization, teardown, reporting, CPU-side resource wrappers, state tracking, query timing, viewport state, and unsupported-feature diagnostics.
5. Add enough Windows standalone player selection/build wiring to instantiate and verify the Vulkan backend locally.
6. Replace placeholder render target and draw submission paths with real Vulkan rendering for the standalone player.
7. Translate or provide the shader path needed by `sample1.imm` playback.

## Implementation tasks

1. [complete] Create `piVulkan_Renderer.h/.cpp` with the full `piRenderer` implementation surface.
2. [complete] Support two initialization paths:
   - externally supplied Vulkan handles through an opaque config struct;
   - self-created Vulkan instance/device/queue for smoke/build validation.
3. [complete] Add minimal Vulkan resource wrappers for textures, buffers, samplers, render targets, shaders, vertex arrays, fixed states, and CPU timing queries.
4. [complete] Add one-time reporter diagnostics for unsupported shader compilation, draw submission, render target operations, image bindings, compute, atomics, and pixel pack buffers.
5. [complete] Register `API::Vulkan` in `piRenderer::Create` for Windows and Android.
6. [complete] Update Windows project files to build the Vulkan source and link/load the Vulkan loader.
7. [complete] Add Windows standalone viewer selection for Vulkan.
8. [complete] Update shared bridge/Godot API names so Vulkan can be requested explicitly on Windows and Android while preserving existing defaults.
9. [in progress] Verify with the closest available Windows standalone build.
10. [complete] Update Android CMake to build the Vulkan source and load `libvulkan.so` dynamically after Windows verification.
11. [complete] Add a separate Windows standalone Vulkan smoke settings file without changing the default viewer config.
12. [in progress] Implement real Vulkan render targets, command buffers, render pass/framebuffer setup, and swapchain presentation for the Windows standalone viewer. The owned Windows path now creates a Win32 `VkSurfaceKHR`, selects a graphics queue that can present to it, enables `VK_KHR_swapchain`, creates a window-sized swapchain, enumerates swapchain images, creates frame command/sync resources, creates swapchain image views, creates a color render pass and per-image framebuffers, and submits/presents swapchain images in `SwapBuffers`; the interim CPU-rendered player color target can now be uploaded through a host-visible staging buffer into the acquired swapchain image. Supported 2D renderer textures now allocate and bind device-local `VkImage` objects and matching `VkImageView` handles, renderer-created render targets now create Vulkan render pass/framebuffer objects over those views, and renderer buffers now create host-visible coherent `VkBuffer` objects for eventual draw binding.
13. [in progress] Implement or generate Vulkan-compatible shaders for the player paths exercised by `sample1.imm`. Static paint vertex/fragment SPIR-V variants are generated and validated for Windows Vulkan; the renderer now creates `VkShaderModule` objects from them, but graphics pipeline/descriptors/draw submission are still pending.
14. [in progress] Verify standalone Vulkan output for `sample1.imm` against the existing base rendering behavior. Nonblank output is now proven, but the current capture is an early partial paint frame and is not yet comparable to the full base renderer.
15. [complete] Implement interim CPU raster output for static paint so `sample1.imm` becomes visible in the Windows standalone Vulkan-selected path before the full SPIR-V/pipeline path lands.

## Follow-up work after this pass

- Define the host-frame contract for Vulkan swapchain images or Godot `RenderingDevice` textures.
- Add GLSL/HLSL-to-SPIR-V shader generation for player layer renderers using the documented Vulkan SDK tools.
- Implement descriptor set layout generation for constants, textures, samplers, and storage buffers.
- Implement render pass/framebuffer compatibility, pipeline cache keys, and draw submission.
- Add Android runtime smoke tests that validate initialization, selected API logging, and a simple nonblank Vulkan render once draw paths exist.

## Verification notes

- Windows `libImmCore` Debug builds with `piVulkan_Renderer.cpp`.
- Windows `appImmViewer` Debug builds successfully with Vulkan selectable from settings.
- Existing OpenGL standalone smoke reached IMM CPU/GPU load and playback start before manual termination.
- Vulkan standalone smoke using `code/appImmViewer/exe/settings-vulkan-smoke.json` initialized an owned Vulkan device and loaded `sample1.imm`, but did not render before the interim paint/present work. The placeholder diagnostics for render target binding and draw submission are now tracked as blockers.
- Vulkan standalone smoke now reaches static paint CPU rasterization for `sample1.imm`: diagnostic `IMM_VK_CPU` reported projected paint vertices, inside-target samples, visible segments, max alpha 1.0, and a nonblack paint target. The generated capture is visible but currently only an early partial frame, so comparable full-output verification remains in progress.
- Current learning: rasterizing every triangle-strip index as a line sample is redundant and slow for static paint. The interim CPU path is being narrowed to unique source paint points per chunk and throttled presentation so the standalone player can progress toward a fuller frame.
- Current learning: the capture hook initially wrote only the first nonblack paint target, which is too weak for comparable-output verification. `IMM_VULKAN_CPU_CAPTURE_OVERWRITE` now allows smoke tests to keep the latest nonblack paint output.
- Latest Windows Vulkan smoke with overwrite capture ran `sample1.imm` for 45 seconds, produced a visibly fuller paint capture, and had no `not implemented`, render-target placeholder, or draw-submission diagnostics in `debug.txt`. Capture stats: 909,307 nonblack pixels, 127,345 near-visible pixels, max RGB 208,204,173.
- Base-renderer comparison is still not fully established locally: OpenGL and DirectX validation capture attempts both exited with `0x80000003` after import-time/runtime issues before producing a validation image. Comparable-output verification remains in progress until a stable base capture or a stronger visual invariant is available.
- Real Vulkan presentation progress: the Windows standalone owned-device path now requests `VK_KHR_surface`/`VK_KHR_win32_surface`, creates a Win32 `VkSurfaceKHR`, requires queue-family surface support, enables `VK_KHR_swapchain` on the logical device, creates a swapchain, enumerates swapchain images, and records/submits/presents a cleared swapchain frame.
- Latest Windows Vulkan smoke verified the real present path: `debug.txt` reported `Vulkan renderer created frame resources` and `Vulkan renderer presented swapchain frame`, then continued to `Loaded in GPU [0]` for `sample1.imm`.
- Latest Windows Vulkan smoke verified player output through the real present path: after `sample1.imm` loaded and paint rasterization produced a nonblack target, `debug.txt` reported `Vulkan renderer presented swapchain nonblack texture frame`. The runtime GDI blit bridge has been removed; PPM capture remains only as an opt-in verification artifact, while visible paint progress is routed through staging upload and `vkQueuePresentKHR`.
- Latest no-GDI Windows Vulkan smoke verified the same route: `debug.txt` reported `Vulkan renderer presented swapchain nonblack texture frame`; opt-in capture stats were 755,024 nonblack pixels, 160,558 near-visible pixels, max RGB 209,227,217.
- Current implementation progress: supported 2D texture formats now map to Vulkan formats and allocate device-local `VkImage` memory through dynamically loaded `vkCreateImage`, `vkGetImageMemoryRequirements`, and `vkBindImageMemory`; those images now get `VkImageView` handles through dynamically loaded `vkCreateImageView`.
- Latest Windows Vulkan smoke after texture image allocation: single-file compile passed, full `appImmViewer` Debug build passed, and `settings-vulkan-smoke.json` loaded `sample1.imm`, created Win32 surface/swapchain/frame resources, reported `Vulkan renderer created VkImage-backed texture`, and presented a nonblack swapchain texture frame. Opt-in PPM capture stats: 263,504 nonblack pixels, 49 near-visible pixels, max RGB 37,50,20. This proves the image allocation change did not break startup/present, but comparable full-output verification remains open because the captured frame is weaker than the prior paint capture.
- Latest Windows Vulkan smoke after texture image-view allocation: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, reported `Vulkan renderer created VkImage-backed texture view`, and presented a nonblack swapchain texture frame. Opt-in PPM capture stats: 802,810 nonblack pixels, 168,636 near-visible pixels, max RGB 207,227,217. This is back in the same range as the stronger prior paint captures, but still relies on the interim CPU paint raster/staging path rather than real Vulkan graphics pipelines.
- Latest Windows Vulkan smoke after swapchain render-pass/framebuffer setup: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, reported `Vulkan renderer created swapchain render pass and framebuffers`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 921,600 nonblack pixels, 121,274 near-visible pixels, max RGB 166,166,53. This verifies the new frame structure does not regress the visible `sample1.imm` playback checkpoint.
- Latest Windows Vulkan smoke after renderer-created render target framebuffer setup: an initial smoke failed because the viewer's main render textures are multisampled and the Vulkan texture helper was still skipping multisample images. After adding Vulkan sample-count handling for 2x/4x/8x/16x images, full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer created render target framebuffer`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 921,600 nonblack pixels, 16,624 near-visible pixels, max RGB 166,166,53.
- Latest Windows Vulkan smoke after renderer buffer backing: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer created VkBuffer-backed renderer buffer`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 790,522 nonblack pixels, 166,962 near-visible pixels, max RGB 207,227,217. The visible path still uses interim CPU paint rasterization, but static paint vertex/index/constant buffers now have real Vulkan buffer handles ready for draw binding.
- Latest Windows Vulkan smoke after static paint SPIR-V generation: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer created SPIR-V shader modules`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 326,309 nonblack pixels, 15,923 near-visible pixels, max RGB 106,227,218. This verifies shader module creation and preserves visible playback, but rendering still uses the interim CPU paint raster path rather than Vulkan graphics pipelines.
- Current graphics-pipeline learning: static paint will need descriptor bindings for layer/display/chunk constant buffers at 3/4/9, vertex storage data at 8, and the blue-noise sampler array at 7. The Vulkan texture helper is being extended from plain 2D images to 2D-array images/views so the blue-noise resource can be bound by the eventual descriptor set.
- Latest Windows Vulkan smoke after 2D-array image/view support: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` created the existing Vulkan resources plus SPIR-V shader modules, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 656,720 nonblack pixels, 125,292 near-visible pixels, max RGB 208,227,217. This verifies the blue-noise 2D-array image/view groundwork did not regress visible playback.
- Current graphics-pipeline learning: combined image-sampler descriptors need a real `VkSampler`; texture creation and explicit sampler creation now create/destroy Vulkan sampler handles using the renderer's texture filter/wrap settings.
- Latest Windows Vulkan smoke after sampler creation: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` created Vulkan resources and SPIR-V modules, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 921,600 nonblack pixels, 181,127 near-visible pixels, max RGB 209,227,217.
- Current graphics-pipeline progress: CPU-backed textures with initial data now upload that data into their Vulkan images through a one-shot staging-buffer command, then transition to `VK_IMAGE_LAYOUT_SHADER_READ_ONLY_OPTIMAL`. This gives the future static-paint descriptor set a populated blue-noise image rather than only CPU-side bytes.
- Latest Windows Vulkan smoke after initial texture-image upload: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 921,600 nonblack pixels, 180,899 near-visible pixels, max RGB 208,227,217.
- Current graphics-pipeline progress: static paint SPIR-V shader creation now also creates a descriptor set layout and pipeline layout matching the player binding contract: layer/display/chunk uniform buffers at 3/4/9, vertex storage buffer at 8, and blue-noise combined image sampler at 7. This is still layout groundwork only; descriptor allocation/update, graphics pipeline creation, and `vkCmdDrawIndexed` submission remain open.
- Latest Windows Vulkan smoke after static paint descriptor/pipeline layout creation: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer created static paint descriptor and pipeline layouts`, reported `Vulkan renderer created SPIR-V shader modules`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced an opt-in PPM capture with 921,600 nonblack pixels, 128,615 near-visible pixels, max RGB 209,207,173.
- Current graphics-pipeline progress: the static paint descriptor pool/set is now allocated from the layout and updated during `DrawPrimitiveIndexed` from the active renderer bindings. The descriptor set now points at the layer/display/chunk uniform buffers, static paint vertex storage buffer, and uploaded blue-noise image/sampler. This is still descriptor binding groundwork; graphics pipeline creation and `vkCmdDrawIndexed` submission remain open.
- Latest Windows Vulkan smoke after descriptor set allocation/update: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer updated static paint descriptor set`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced a complete opt-in PPM capture with 921,600 nonblack pixels, 2,330 near-visible pixels, max RGB 155,156,44.
- Current graphics-pipeline progress: static paint now creates a real Vulkan graphics pipeline for the active SPIR-V shader modules and renderer-created render pass. The pipeline uses a triangle-strip input assembly, no vertex attributes because static paint reads vertex data through binding 8, dynamic viewport/scissor, raster state derived from the current paint raster state, alpha blending, the static paint pipeline layout, and the render target's sample count. Command-buffer render-pass recording, descriptor/pipeline binding, index binding, and `vkCmdDrawIndexed` remain open.
- Latest Windows Vulkan smoke after graphics pipeline creation: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer created static paint graphics pipeline`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced a complete opt-in PPM capture with 921,600 nonblack pixels, 2,330 near-visible pixels, max RGB 155,156,44.
- Current graphics-pipeline progress: static paint now records and submits a real Vulkan command buffer for the active draw call. The submitted commands begin the renderer-created render pass, set dynamic viewport/scissor, bind the static paint graphics pipeline, bind the static paint descriptor set, bind the index buffer, issue `vkCmdDrawIndexed`, end the render pass, submit to the graphics queue, and wait for completion. The CPU raster fallback still runs afterward and remains the visible presentation source, so GPU-rendered output is not yet independently verified.
- Latest Windows Vulkan smoke after static paint draw submission: full `appImmViewer` Debug build passed, `settings-vulkan-smoke.json` reported `Vulkan renderer submitted static paint draw commands`, loaded `sample1.imm`, reported `Vulkan renderer presented swapchain nonblack texture frame`, and produced a complete opt-in PPM capture with 921,600 nonblack pixels, 2,330 near-visible pixels, max RGB 155,156,44.
- Android `:libImmCore:assembleDebug` builds successfully with `piVulkan_Renderer.cpp` included; CMake still emits the pre-existing dev warning about no top-level `project()` command.
